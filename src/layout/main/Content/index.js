import Box from "@mui/material/Box";
import gradient from "util/gradient";
function Content() {
  return <Box sx={{ background: gradient.background, height: "100%" }}></Box>;
}

export default Content;
